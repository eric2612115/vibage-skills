# app-a

Synthetic checked_out root for P1 smoke and Focus agent-pressure.

**Purpose:** Minimal diggable surface for locate Nested / Mode evidence.  
**Entry hint:** `src/app.py` exposes `health()` → `"ok"`.

Not a real business app. Agents copy this tree into an isolated workspace before dig.

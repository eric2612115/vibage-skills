# hub-seeds

Optional SCAN_PLAN / handoff seed snippets for Focus agents.

- Copy into an **isolated** workspace hub after `install.sh --init-hub`.
- Never treat these as product `docs/vibage/` on the package tree.
- Card3 terminal envelope: derive from `tests/fixtures/run_failed_handoff.json` (package fixture); seed into workspace RUNS only.

---
description: Vibage hard stops pointer (thin; prefer adapters/cursor/vibage.mdc)
alwaysApply: false
---

# Vibage — hard stops (legacy thin rule)

Prefer `adapters/cursor/vibage.mdc` (install copies to `.cursor/rules/vibage.mdc`).

- Resolve PKG_ROOT via `$PKG_ROOT/scripts/resolve-pkg-root.sh` (or probe Cursor/Claude/Codex skill homes).
- Obey `$PKG_ROOT/references/hard-stops.md`.
- Nested: `$PKG_ROOT/references/nested-protocol.md` (capability matrix; degrade honestly).
- Dual reports: `VIBAGE-ISSUE-OWNER.md` + `VIBAGE-ISSUE-LOCATE.md` at workspace root after gates.

# Coverage — multi-IDE first wave (2026-07-22)

## Scope

Cursor + Claude Code + Codex install surfaces. Copilot / Windsurf = out of scope.

## Checklist

| # | Check | Pass? |
|---|-------|-------|
| 1 | `install.sh --surfaces=cursor,claude,codex` links MANIFEST skills into `~/.cursor/skills`, `~/.claude/skills`, `$HOME/.agents/skills` | PASS |
| 2 | `--with-project-rule=<tmp>` creates `.cursor/rules/vibage.mdc`, upserts `AGENTS.md` + `CLAUDE.md` vibage blocks, writes `.claude/vibage-entry.md` | PASS |
| 3 | `--project-skills=<tmp>` links under `.cursor/skills`, `.claude/skills`, `.agents/skills` | PASS |
| 4 | `--init-hub=<tmp>` writes `docs/vibage/STATUS.md` (+ hub files); second run skips without `--force-hub` | PASS |
| 5 | `resolve-pkg-root.sh` finds PKG_ROOT when only Claude or only Codex global links exist | PASS |
| 6 | `verify-pins.sh` passes when superpowers is present in any probed home | PASS |
| 7 | NEW-CHAT routes init → orient → locate (dispatcher; no nested paste) | PASS (content review) |
| 8 | Nested unavailable → `Mode: degraded` still delivers dual reports (manual / product lock) | PASS (protocol lock) |

## Happy-path notes (per surface)

- **Cursor:** paste NEW-CHAT; skills under `~/.cursor/skills`; Task for nested when available.
- **Claude Code:** same NEW-CHAT; skills under `~/.claude/skills`; Agent/subagents when available.
- **Codex:** same; skills under `$HOME/.agents/skills`; subagents when available.

## Evidence log

- 2026-07-23: `install.sh` three-surface global + temp project + SelfAutoBuz refresh — ALL CHECKS PASSED
- `resolve-pkg-root.sh` with fake HOME (Claude-only / Codex-only) → `/Users/eric.fang/MindOwnBuz/vibage-skills`
- `verify-pins.sh` OK at pin `363923f74aa9cd7b470c0aaa73dee629a8bfdc90`; also OK when only `~/.claude/skills/superpowers` present under fake HOME

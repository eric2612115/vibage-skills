---
name: vibage-map-deepen
description: >-
  Legacy name kept for one release. Optional nested notes after GRAPH_FLOOR_OK /
  PILE_INDEX_OK now route to W3a dimension-fill (ledger dimension_* claims).
  Never claim MAP_DEEPEN_OK. Success = DIMENSION_FILL_OK / PARTIAL / BLOCKED from
  verify-dimension-fill.sh. Not Plan-L Mermaid/Graphify. Not Architecture Pass.
  Not system-understood. Not Gate A full-sweep. Not dig/CONFIRM substitute. ∉ Tier-0.
---

# Vibage Map Deepen → Dimension Fill (migrated)

**Status:** thin pointer. `MAP_DEEPEN_OK` brand **retired** (W3a P2).  
**Do this instead:** owner consent + `scripts/dimension-fill.sh` / `verify-dimension-fill.sh` → parse `DIMENSION_FILL_*` tokens (`exit 0 ≠ DIMENSION_FILL_OK`).

**Name lock:** This skill ≠ Plan-L「local-maps deepen」(Mermaid/Graphify prettier).

**Honest scope:** Ledger `dimension_*` notes ≠ Architecture Pass ≠ CONFIRM ≠ dig-all ≠ Gate A full-sweep ≠ full-understanding ≠ dig-ready-after-install.

## When / Not

| When | Not |
|------|-----|
| Parent hub + floor; owner **yes** to dimension-fill after anti-illusion + cost band | Default deepen/fill on every install |
| Freeze uses `dimension_yes` + `dimension_scope_ids` (not `deepen_yes` alone) | Silent auth via legacy `deepen_*` / `pipeline_id=map_deepen` |
| Script verify prints `DIMENSION_FILL_*` | Never claim `MAP_DEEPEN_OK` or “system understood” |
| Ticket paste alone | Implicit yes (ticket = implicit **no**) |

## PKG_ROOT

```bash
bash /path/to/vibage-skills/scripts/resolve-pkg-root.sh
```

## Procedure (MUST)

1. Anti-illusion already said (nameplate ≠ understood; ≠ dig-ready; ≠ full-sweep).
2. Cost band for N repos; owner **yes** dimension-fill (not deepen slogan).
3. Freeze DECISIONS with `dimension_yes` / `dimension_scope_ids` / classes per design §4.
4. Run:

```bash
bash "$PKG_ROOT/scripts/dimension-fill.sh" "<mother>" [--claims-dir=<path>]
# or verify only:
bash "$PKG_ROOT/scripts/verify-dimension-fill.sh" "<mother>"
```

5. Parse exact token. Never print or claim `MAP_DEEPEN_OK`.
6. `verify-map-deepen.sh` is a migrate shim only — legacy envelopes do not greenwash.

## Forbidden

- green-shrink after partial to mint OK
- narrating understood / dig-ready / ready-after-install-alone / full-sweep from fill or deepen
- wiring into Tier-0 / pack-health / `assert_gate`
- treating dossiers stub alone as depth without ledger claim ids

# SAT-agent-pressure (outline stub)

**Owns:** Focus protocol details + scenario library (later).  
**STATUS row:** Focus: agent-pressure — Designed=YES, On-tree=stub, Proven-green=NO.  
**Scope:** `agent` meta — **not** a `pipeline_id` (≠ locate / fix / 架構檢視).

## Protocol pointer (thin)

- Focus observes handoff honesty (`handoff_honored` reserved) and agent-pressure scenarios.
- Does **not** mint product pipeline runs by itself.
- Do **not** mark Proven-green until agent RED→GREEN scenarios exist and pass.

## Out of scope (this stub)

Scenario library, agent E2E harness, fake CI green for Focus.

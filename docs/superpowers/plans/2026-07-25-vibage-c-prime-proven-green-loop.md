# C′ Proven-green Loop Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans. Checkbox tracking.

**Goal:** Honestly set STATUS C′ row **Proven-green=YES** (loop stop). PRESSURE_PASS alone is insufficient.

**Architecture:** STATUS C′ **Scope=`script+live-pressure`** (carve-out, Focus-like). Suite proves graph/brief/ledger **script** gates; live parents prove continuum entry→floor→matrix under agent panel. **letter B stays NO**. Sync / dimension / Tier-0 deferred. Claim must **not** say “full graph/brief/ledger agent-proven” — brief/ledger remain script-proven via suite; live panel = continuum honesty.

**Tech Stack:** existing `tests/test_c_prime_*.sh` + git-safe evidence index under `docs/evidence/c-prime/`.

---

## LOOP_PASS (= stop)

All must hold:

1. **Script suite:** every `tests/test_c_prime_*.sh` exits 0 (orchestrator `tests/test_c_prime_suite.sh` prints `C_PRIME_SUITE_OK`).
2. **Live pressure (mandatory fresh run before flip):** DefiStrategy + MindOwnBuz both PRESSURE_PASS on the **same** `run_ts` written into SUMMARY (no reuse of stale R2 alone).
3. **Evidence on tree:** `docs/evidence/c-prime/SUMMARY.md` binds that `run_ts` only; panel tally; mechanical tokens; explicit NOT claims.
4. **STATUS:** Proven-green=YES, Scope=`script+live-pressure`, C′ carve-out paragraph; freeze-lift splits Proven-green vs letter B; design header updated. Banner: ≠ letter B ≠ 掃透 ≠ sync/dimension/Tier-0.
5. After flip: clean live hubs (`docs/vibage`); keep project entry.

**Explicitly NOT required:** substantive 掃透 on full DefiStrategy; sync §2.5; dimension fill; Tier-0 membership; letter B.

**Loop:** if any fail → clean hubs → amend Gaps → 3-review → implement → suite → pressure → score. Max **5** rounds from this plan. Abort to owner if blocked on non-code (e.g. demand full-pile 掃透).

---

## Gaps vs prior “PRESSURE_PASS stop”

| ID | Gap | Fix |
|----|-----|-----|
| P1 | No git-safe C′ evidence index | Add `docs/evidence/c-prime/SUMMARY.md` |
| P2 | No single suite stamp | Add `tests/test_c_prime_suite.sh` → `C_PRIME_SUITE_OK` |
| P3 | STATUS/freeze/design still NO | Flip after P1+P2+reconfirm pressure |
| P4 | Plan wave1 said “don’t flip without owner” | Owner now requires Proven-green; this plan is authorization |

---

## Tasks

### Task 1: Suite orchestrator

- Create: `tests/test_c_prime_suite.sh`
- Runs all `test_c_prime_*.sh` except itself; prints `C_PRIME_SUITE_OK`
- [ ] Implement + run green

### Task 2: Evidence SUMMARY

- Create: `docs/evidence/c-prime/SUMMARY.md`
- Fields: claim, run_ts, parents, mechanical tokens, panel 8/8, NOT claims
- [ ] Write from R2 (+ R3 if re-run)

### Task 3: Re-pressure confirm (R3)

- Clean hubs → install+c-prime-fill both → 8-agent panel → score PRESSURE_PASS
- [ ] If fail → code Gaps (new) not STATUS flip
- [ ] If pass → proceed Task 4

### Task 4: STATUS honesty flip

- Modify: `STATUS.md` C′ row Proven-green=YES; banner; continuum sentence
- Modify: freeze-lift Proven-green row; design header Status line
- [ ] Keep sync deferred / ∉ Tier-0 / ≠ letter B

### Task 5: Stop or continue

- [ ] Verify LOOP_PASS checklist
- [ ] Clean hubs
- [ ] If FAIL → round++ and amend this plan

---

## Honesty locks

- Never set Proven-green from suite alone without live pressure evidence.
- Never claim 掃透 or letter B from this flip.
- Presence-local ≠ env swept.
- Waiver never grants substantive.

---

## Loop result

**LOOP_PASS achieved** — STATUS C′ Proven-green=YES (`run_ts=20260724T171248Z`). Suite `C_PRIME_SUITE_OK`; R3 panel 8/8; hubs cleaned. Loop stopped.
